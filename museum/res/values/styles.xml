<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
          <item name="android:actionBarStyle">@style/picActionBar</item>
    </style>
    <!-- ActionBar styles -->
   <style name="picActionBar" parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:background">@drawable/topbar</item>
         <item name="android:titleTextStyle">@style/picActionBarTitleText</item>
    </style>
   
    <style name="picActionBarTitleText">
        <item name="android:textColor">#FFFF</item>
        <item name="android:textSize">20sp</item>
    </style>
</resources>